# audio/ — 听觉线（未来，六层阶梯第二模态）

> 状态：**占位**。视觉线（docs/177-201）跑通后启动。

六层阶梯（接收/对齐/模型/维持/否定/被看见）是模态无关的架构主张——听觉是第二模态，
不是新架构。启动时约束：

- **复用 core/**：利害外置、Top-K 威胁度、否定注册、态势地图（跨模态共享件从
  `../core/` 取，不在 audio/ 内重写）；
- **事件化输入**：视觉用事件流，听觉用事件化音频（如 NES 阈值触发的音事件），
  保持"残差是原子"的同一原则；
- **文档续号**：从 `../docs/202` 开始（时间线连续，不另起编号）。

设计参考：docs/178 六层阶梯、docs/186 §六 规模切割（合法扩大三维度）。
